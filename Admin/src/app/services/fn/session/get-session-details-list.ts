/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SessionDetailsListResponse } from '../../models/session-details-list-response';

export interface GetSessionDetailsList$Params {

/**
 * ID de la session
 */
  id: number;
}

export function getSessionDetailsList(http: HttpClient, rootUrl: string, params: GetSessionDetailsList$Params, context?: HttpContext): Observable<StrictHttpResponse<SessionDetailsListResponse>> {
  const rb = new RequestBuilder(rootUrl, getSessionDetailsList.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SessionDetailsListResponse>;
    })
  );
}

getSessionDetailsList.PATH = '/session/list-details/{id}';
