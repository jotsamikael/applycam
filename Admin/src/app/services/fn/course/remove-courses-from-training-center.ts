/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface RemoveCoursesFromTrainingCenter$Params {

/**
 * Numéro d'agrément du centre
 */
  agreementNumber: string;
      body: Array<number>
}

export function removeCoursesFromTrainingCenter(http: HttpClient, rootUrl: string, params: RemoveCoursesFromTrainingCenter$Params, context?: HttpContext): Observable<StrictHttpResponse<string>> {
  const rb = new RequestBuilder(rootUrl, removeCoursesFromTrainingCenter.PATH, 'delete');
  if (params) {
    rb.path('agreementNumber', params.agreementNumber, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<string>;
    })
  );
}

removeCoursesFromTrainingCenter.PATH = '/course-management/courses/{agreementNumber}';
