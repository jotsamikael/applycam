/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CandidateResponse } from '../../models/candidate-response';

export interface FindCandidateByEmail$Params {
  email?: string;
}

export function findCandidateByEmail(http: HttpClient, rootUrl: string, params?: FindCandidateByEmail$Params, context?: HttpContext): Observable<StrictHttpResponse<CandidateResponse>> {
  const rb = new RequestBuilder(rootUrl, findCandidateByEmail.PATH, 'get');
  if (params) {
    rb.query('email', params.email, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CandidateResponse>;
    })
  );
}

findCandidateByEmail.PATH = '/candidate/find-by-email';
