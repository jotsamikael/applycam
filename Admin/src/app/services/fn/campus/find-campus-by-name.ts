/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CampusResponse } from '../../models/campus-response';

export interface FindCampusByName$Params {
  name: string;
}

export function findCampusByName(http: HttpClient, rootUrl: string, params: FindCampusByName$Params, context?: HttpContext): Observable<StrictHttpResponse<CampusResponse>> {
  const rb = new RequestBuilder(rootUrl, findCampusByName.PATH, 'get');
  if (params) {
    rb.path('name', params.name, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CampusResponse>;
    })
  );
}

findCampusByName.PATH = '/campus/find-campus/{name}';
