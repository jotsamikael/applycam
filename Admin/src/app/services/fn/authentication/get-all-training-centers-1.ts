/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseTrainingCenterResponse } from '../../models/page-response-training-center-response';

export interface GetAllTrainingCenters1$Params {
  offset?: number;
  pageSize?: number;
  field?: string;
  order?: boolean;
}

export function getAllTrainingCenters1(http: HttpClient, rootUrl: string, params?: GetAllTrainingCenters1$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseTrainingCenterResponse>> {
  const rb = new RequestBuilder(rootUrl, getAllTrainingCenters1.PATH, 'get');
  if (params) {
    rb.query('offset', params.offset, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('field', params.field, {});
    rb.query('order', params.order, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseTrainingCenterResponse>;
    })
  );
}

getAllTrainingCenters1.PATH = '/auth/get-all-trainingCenter';
