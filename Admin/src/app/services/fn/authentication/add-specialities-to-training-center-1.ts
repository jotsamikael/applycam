/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AddSpecialitiesToTrainingCenter1$Params {
  agreementNumber: string;
      body: Array<number>
}

export function addSpecialitiesToTrainingCenter1(http: HttpClient, rootUrl: string, params: AddSpecialitiesToTrainingCenter1$Params, context?: HttpContext): Observable<StrictHttpResponse<string>> {
  const rb = new RequestBuilder(rootUrl, addSpecialitiesToTrainingCenter1.PATH, 'patch');
  if (params) {
    rb.query('agreementNumber', params.agreementNumber, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<string>;
    })
  );
}

addSpecialitiesToTrainingCenter1.PATH = '/auth/link-to-center';
