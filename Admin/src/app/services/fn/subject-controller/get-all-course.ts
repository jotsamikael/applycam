/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseSubjectResponse } from '../../models/page-response-subject-response';
import { SubjectRequest } from '../../models/subject-request';

export interface GetAllCourse$Params {
  subjectRequest: SubjectRequest;
  offset?: number;
  pageSize?: number;
  field?: string;
  order?: boolean;
}

export function getAllCourse(http: HttpClient, rootUrl: string, params: GetAllCourse$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseSubjectResponse>> {
  const rb = new RequestBuilder(rootUrl, getAllCourse.PATH, 'get');
  if (params) {
    rb.query('subjectRequest', params.subjectRequest, {});
    rb.query('offset', params.offset, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('field', params.field, {});
    rb.query('order', params.order, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseSubjectResponse>;
    })
  );
}

getAllCourse.PATH = '/subjects/get-all';
