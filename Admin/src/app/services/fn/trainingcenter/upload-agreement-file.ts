/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface UploadAgreementFile$Params {

/**
 * Numéro d'accord
 */
  'agreement-number': string;
  fileType: string;
      body?: {

/**
 * Fichier d'accord à télécharger
 */
'file': Blob;
}
}

export function uploadAgreementFile(http: HttpClient, rootUrl: string, params: UploadAgreementFile$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, uploadAgreementFile.PATH, 'post');
  if (params) {
    rb.path('agreement-number', params['agreement-number'], {});
    rb.query('fileType', params.fileType, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

uploadAgreementFile.PATH = '/trainingcenter/agreement/{agreement-number}';
